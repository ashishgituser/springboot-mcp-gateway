spring:
  application:
    name: mcp-gateway-sample

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always

# mcp:
#   gateway:
#     mcp-endpoint: /mcp
#     servers:
#       - id: filesystem
#         endpoint: http://localhost:8081/mcp
#
# Auth/policy and rate-limiting configuration land here in later phases (see project README).

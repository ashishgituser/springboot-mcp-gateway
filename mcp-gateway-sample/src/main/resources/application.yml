spring:
  application:
    name: mcp-gateway-sample

# mcp:
#   gateway:
#     mcp-endpoint: /mcp
#     servers:
#       - id: filesystem
#         endpoint: http://localhost:8081/mcp
#
# Auth/policy, observability and rate-limiting configuration land here in later phases
# (see project README).

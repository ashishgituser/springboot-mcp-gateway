spring:
  application:
    name: mcp-gateway-sample

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
  endpoint:
    health:
      show-details: always

# mcp:
#   gateway:
#     mcp-endpoint: /mcp
#     servers:
#       - id: filesystem
#         endpoint: http://localhost:8081/mcp
#     rate-limit:
#       enabled: true
#       capacity: 100
#       refill-tokens: 100
#       refill-period: 1m
#
# Auth/policy configuration lands here in a later phase (see project README).

/**
 * Marker package for the starter module. This module carries no code of its own - it exists to pull
 * in {@code mcp-gateway-autoconfigure} plus the transitive dependencies a Spring Boot application
 * needs, the same way any other Spring Boot starter does.
 */
package io.github.ashishgituser.mcpgateway.starter;

/**
 * Domain logic for the MCP gateway: routing/aggregation, policy evaluation, rate-limit SPI and the
 * audit model. This module has no dependency on Spring Boot autoconfiguration so it can be reused
 * outside a Spring context if needed.
 */
package io.github.ashishgituser.mcpgateway.core;
